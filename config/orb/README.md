# ORB 配置（V2）

- `v2/symbols.txt` — 默认标的池（43 标）
- `v2/live_gate.json` — Gate 回退（优先 `data/orb/live/live_gate.json`）
- 标的：**`ORB_V2_SYMBOLS`** 或 **`ORB_V2_SYMBOLS_FILE`**（默认 `data/orb/ml/symbols/universe.txt`）
