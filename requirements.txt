# Next K API — OI / accumulation / ZCT VWAP (no Kronos / PyTorch)
# =====================

# Core
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# Data
numpy>=1.24.0
pandas>=2.0.0
requests>=2.31.0
apscheduler>=3.10.0
pytz>=2023.3

# Exchange
ccxt>=4.0.0
aiohttp>=3.9.0

# Multi-Asset Data (Stocks & Forex) — used by /api/radar OHLCV fetch
yfinance>=0.2.0

# Utils
tqdm>=4.65.0

# HTTP client (s1 binance alpha monitor)
httpx>=0.27.0
