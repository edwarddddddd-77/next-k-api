# vnpy KK 实盘链路（可选，与 requirements.txt 分开安装）
# 安装：pip install -r requirements-vnpy.txt
# vnpy 核心使用仓库内 vendor/vnpy（由 quant.engine.bootstrap 注入路径）
# 实盘交易所：VNPY_LIVE_EXCHANGE=binance|bitget（默认 binance）

vnpy_ctastrategy>=1.3.0
vnpy_sqlite>=1.1.0
vnpy_binance>=2025.1.0
# Bitget 实盘（VNPY_LIVE_EXCHANGE=bitget 时安装社区插件，例如 vnpy_bitgets）
# vnpy_bitgets  # pip install -e git+https://github.com/xldistance/vnpy_bitgets.git
vnpy-rest>=1.0.0
vnpy-websocket>=1.0.0
tzdata>=2024.1
